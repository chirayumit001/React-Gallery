import { FC, ReactNode, useEffect, useState } from 'react'
// import styles from '../styles/Home.module.scss'
import images from './images'

const NavButtons = () => {
	const len = images.length

	return <div>button*len</div>
}
export default NavButtons
